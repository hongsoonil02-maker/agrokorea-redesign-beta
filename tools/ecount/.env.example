# Copy to project-root .env and fill real values. NEVER commit .env.
COM_CODE=          # 이카운트 회사번호 (예: 662778)
USER_ID=           # 실제 이카운트 로그인 계정 ID (마스터 권장) — 키 발급ID가 아님!
API_CERT_KEY=      # 관리자 > API 인증키에서 발급된 키
LAN_TYPE=ko-KR
